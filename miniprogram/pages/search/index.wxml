<view class="page">
  <view class="search-row">
    <van-icon name="arrow-left" size="36rpx" color="#111" catch:tap="onBack" />
    <view class="search-box">
      <van-icon name="search" size="32rpx" color="#7a7e83" />
      <input value="{{ keyword }}" placeholder="搜索服务 / 帖子 / 公告 / 功能" focus bindinput="onInput" />
    </view>
  </view>

  <view class="tabs">
    <view wx:for="{{ tabs }}" wx:key="*this" class="tab {{ activeType === item ? 'tab--active' : '' }}" data-key="{{ item }}" catch:tap="onTabTap">{{ item }}</view>
  </view>

  <view wx:if="{{ !keyword }}" class="hot-panel">
    <view class="section-row">
      <text class="section-title">热门搜索</text>
    </view>
    <view class="hot-row">
      <text wx:for="{{ hotWords }}" wx:key="*this" class="hot-word" data-word="{{ item }}" catch:tap="onHotTap">{{ item }}</text>
    </view>
  </view>

  <view wx:if="{{ !keyword && historyWords.length }}" class="hot-panel">
    <view class="section-row">
      <text class="section-title">历史搜索</text>
      <text class="clear-link" catch:tap="onClearHistory">清空</text>
    </view>
    <view class="hot-row">
      <text wx:for="{{ historyWords }}" wx:key="*this" class="hot-word" data-word="{{ item }}" catch:tap="onHotTap">{{ item }}</text>
    </view>
  </view>

  <view class="result-list">
    <view wx:if="{{ !results.length }}" class="empty">
      <van-icon name="search" size="88rpx" color="#b7c0ca" />
      <text>没有找到相关内容</text>
    </view>
    <view wx:for="{{ results }}" wx:key="id" class="result-card" data-id="{{ item.id }}" catch:tap="onResultTap">
      <text class="result-type">{{ item.type }}</text>
      <view class="result-main">
        <text class="result-title">{{ item.title }}</text>
        <text class="result-desc">{{ item.desc }}</text>
      </view>
      <van-icon name="arrow" size="26rpx" color="#999" />
    </view>
  </view>
</view>
